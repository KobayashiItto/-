<h1>広大グルメ</h1>
<h3>詳細</h3>
<div class="post">
  <p><%= @post.title %></p>
  <p><%= @post.about %></p>
  <p><%= @post.category %></p>
  <p><%= @post.url %></p>
  <p><%= @post.created_at %></p>
</div>

<%= link_to "一覧に戻る", posts_path %>
<%= link_to "編集する", edit_post_path(@post.id) %>